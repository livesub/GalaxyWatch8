# 작업 진행 로그

이 파일은 세션 끊길 때(사용량 제한 등) 다음 세션에서 이어서 작업하기 위한 상태 기록임.
새 세션 시작 시: "CLAUDE.md + PROGRESS.md 확인하고 이어서 해줘" 라고 요청하면 됨.

## 전체 단계 체크리스트
- [~] 1. 프로젝트 골격 (build.gradle, AndroidManifest.xml, 폴더구조) — 골격만 완료, 디지털 UI 미구현
- [x] 2. 시계/날짜/요일 (메인시계, 날짜, 요일 7색)
- [x] 3. 날씨 바인딩 (온도, 날씨아이콘 day/night 32종 조건분기)
- [x] 4. 콤플리케이션 슬롯 6개 (차량/AI/폰배터리/음성녹음/일몰/일출)
- [x] 5. 하단 데이터 바인딩 (워치배터리, 걸음수, 심박수·스트레스)
- [x] 6. 배경 (주/야 자동전환, 원형크롭) — 날씨조건별 세분화는 5-17번 확정에 따라 제외(중앙 아이콘이 담당)
- [x] 7. AOD 레이아웃 (시:분만)
- [x] 8. 리소스 변환 (PNG→WebP, 리사이즈)
- [x] 9. 빌드/에뮬레이터 테스트
- [x] 10. 실기기(워치) 테스트 준비(절차 정리) — 실제 워치 설치/확인은 사용자가 진행
- [x] 11. WFF Validator 검증

## 마지막 세션 상태
- 날짜: 2026-08-27
- 마지막으로 작업한 단계: 5단계(배경 연결) 완료
- 5단계 완료 내역:
  - `bg_day.webp`/`bg_night.webp`(438x438, 원형 크롭 확인됨—네 모서리 alpha=0) 사용자가 root `src/main/res/drawable`에 넣은 걸 `watchface/src/main/res/drawable`(프로덕션 경로)로 이동
  - watchface.xml: `WEATHER.IS_DAY` Condition으로 배경 PartImage 분기, Scene 맨 앞(최하단 레이어)에 배치해서 다른 요소들이 위에 그려지도록 구성
  - 빌드 성공
- 이전 완료된 것(4단계):
- 4단계 완료 내역:
  - 온도: `WEATHER.TEMPERATURE_UNIT`(1=섭씨,2=화씨)로 Condition 분기 → 화씨면 `(F-32)*5/9` 변환, 섭씨면 그대로 표시. 섭씨(℃) 강제 고정 가능함을 확인(6-3 미결 항목 해소)
  - 날씨 아이콘: `WEATHER.IS_DAY` × `WEATHER.CONDITION`(0~15) 중첩 Condition으로 day/night 아이콘 32종 조건분기, docs/weather_icons.md 매핑표 그대로 반영(동일 아이콘 매핑되는 코드는 OR로 묶어 11개 Compare로 축약)
  - 아이콘은 각 webp 원본 비율(트림 후 최대변 130px) 그대로 중앙(x≈219,y≈158 앵커) 배치 — 늘리기/찌그러짐 없음
  - 빌드 성공
- 주의: `[WEATHER.IS_DAY]`를 비교 연산 없이 boolean 값 그대로 Condition expression에 사용함 — 공식 예시로 직접 확인 못한 부분이라, 13/15단계 실제 검증에서 안 먹히면 `== true`로 수정 필요
- 이전 완료된 것:
  - `watchface/build.gradle.kts`: applicationId `com.keingma.watch`, minSdk 34, versionName "1.0"
  - 날씨아이콘 32종: 투명 여백 제거 + 최대변 130px 리사이즈 + WebP(무손실) 변환, `watchface/src/main/res/drawable`에 적용(3.9MB→512KB). 원본 PNG는 `src/main/design/weather_icons_source/`에 보관
  - root `assets/`(빈 폴더) 삭제
  - watchface.xml: 기존 아날로그 뼈대(다이얼/눈금/시분초침) 전부 제거, CLOCK_TYPE을 DIGITAL로 변경
  - watchface.xml: 날짜(yyyy.MM.dd) + 요일(월~일 7색, Condition/DAY_OF_WEEK 분기) + 메인시계(TimeText hh:mm, hourFormat=12) + 오전/오후 접두(Condition/AMPM_STATE 분기) + 초(TimeText ss, 보조) 구현. 전 텍스트 흰색+다크 아웃라인(rgba(0,0,0,0.7)), SYNC_TO_DEVICE 폰트(BOLD) — 시스템 폰트 폴백으로 Noto Sans KR Bold 렌더링 기대(6-2)
  - orgi.png(1024x1024) 비율 환산으로 좌표 배치, 원형 캔버스 내 클리핑 안전 범위 계산 완료
  - `gradlew :watchface:assembleRelease` 빌드 성공
- 막힌 것 / 에러: 없음
- 주의(다음 세션 필독):
  - **res/raw는 AAPT2가 파싱하지 않는 원시 리소스라 Gradle 빌드 성공이 watchface.xml의 WFF 스키마 정합성을 보장하지 않음.** TimeText/PartText/Text/Font/Outline/Template/Condition 문법은 공식 문서(developer.android.com/reference/wear-os/wff/...) 직접 조회로 검증했지만, 실제 스키마 검증은 15단계 WFF Validator 또는 13단계 에뮬레이터 로드에서 최종 확인 필요.
  - 요일/오전오후 색상·문구는 Condition expression([DAY_OF_WEEK], [AMPM_STATE]) 기반, 값 매핑은 CLAUDE.md 6-1 순서와 Java Calendar 관례(1=일~7=토) 기준으로 구현.
- 다음에 할 일: 6단계(콤플리케이션 슬롯 1~2, 앱 바로가기) 진행

## 에뮬레이터 조기 검증 (2026-08-27, 9단계 선행 확인 — 1~6단계分만 대상, 9단계 자체는 미완료 상태 유지)
- AVD `GalaxyWatch8_Test`(Wear OS 5)에 `:watchface:assembleDebug` 설치 후
  `adb shell am broadcast -a com.google.android.wearable.app.DEBUG_SURFACE --es operation set-watchface --es watchFaceId com.keingma.watch`로 활성화, 스크린샷으로 실제 렌더 확인
- **버그 발견 및 수정**: `<TimeText>`를 `<Scene>` 바로 아래 단독으로 넣으면 조용히 렌더링 안 됨(로그 에러 없음, 그냥 안 보임) → 공식 예제처럼 `<DigitalClock>`으로 감싸야 함. 메인시계+초 TimeText 둘 다 DigitalClock으로 래핑해서 해결, 재검증 완료(실제 시각 "오전 06:31 13" 정상 표시)
- 나머지 검증된 것: 배경(day 그라디언트+원형크롭), 날짜/요일색("2026.08.27 목" 목요일 주황 정상), 날씨아이콘 폴백(Default 분기 cloudy_plain 정상 표시), Condition/Outline/Font/PartImage/PartText 전부 정상 파싱·렌더 확인
- 확인 못한 것(에뮬레이터 한계): WEATHER.* 실데이터(위치 없어서 온도 0°C·아이콘은 Default 폴백만 확인, CLAUDE.md 6-6 예상대로) — 실기기에서 재확인 필요
- 결론: 1~6단계 XML 문법은 실기기 렌더 기준 유효함 확인됨(이전 세션들의 "res/raw 미검증" 우려 상당 부분 해소)

## 오전/오후 세로 배치 변경 (2026-08-27)
- CLAUDE.md 6-3 정정: 오전/오후 가로→세로(오/전, 오/후 글자 쌓기), 오전 #4FC3F7 / 오후 #FFA726, 다크 아웃라인 유지
- watchface.xml: `Group`으로 PartText 2개(위/아래) 묶어서 메인시계 높이(70)에 맞춰 배치, `AMPM_STATE` Condition 색상도 함께 분기
- 에뮬레이터 재검증 완료(스크린샷: "오/전" 하늘색 세로 정상 표시)
- 참고: 뜬 구름 아이콘은 `day_10_cloudy_plain.webp`(정식 매핑, CONDITION 미수신시 Default 폴백) 확인됨

## 6단계: 콤플리케이션 슬롯 1~2 (앱 바로가기) - 2026-08-27
- watchface.xml에 `ComplicationSlot`(slotId 1=SLOT_TOP_LEFT, 2=SLOT_TOP_RIGHT) 추가. `DefaultProviderPolicy defaultSystemProvider="APP_SHORTCUT"`, supportedTypes="MONOCHROMATIC_IMAGE SMALL_IMAGE", 반투명 다크 배경(Ellipse) + 아이콘. `isCustomizable="true"`로 사용자 재지정 허용
- 위치: 날씨아이콘과 같은 높이(y center≈158), 좌/우 원형, 지름 64px
- 빌드(`assembleDebug`) 성공
- **에뮬레이터 검증은 미완료(불확실)**: 설치 후 활성화 로그상 ComplicationSlot 자체는 정상 파싱/등록됨(`[SLOT_TOP_LEFT] Change touchable: true`, provider 상태 NO_DATA→NOT_CONFIGURED 정상 흐름, XML 파싱 에러 없음) — 그런데 `WatchFaceService: createUserStyleSchema... should complete in less than 5000 milliseconds` 경고 이후 시스템이 "recovery" 모드로 빠져 실제 디자인 대신 기본 세이프모드 화면(초록 원형)이 표시됨. 같은 시점에 무관한 앱(캘린더)도 ANR 발생한 것으로 보아 에뮬레이터 자체 리소스 과부하 가능성이 높음(우리 XML 문제로 100% 단정 못함)
- **다음 세션/13단계에서 재확인 필요**: 에뮬레이터 새로 부팅한 깨끗한 상태에서 이 두 슬롯이 실제로 렌더링되는지 재검증할 것
- PROGRESS.md 4번은 6개 슬롯 다 끝나면 체크(지시서 방침) — 아직 미체크

## 7단계: 콤플리케이션 슬롯 3~4 (중단 좌/우) - 2026-08-27
- `ComplicationSlot`(slotId 3=SLOT_MID_LEFT, 4=SLOT_MID_RIGHT) 추가, 날짜 행과 같은 높이(y center≈222), 지름 60px
- SLOT_MID_LEFT: `defaultSystemProvider="EMPTY"`, supportedTypes에 SHORT_TEXT/RANGED_VALUE/MONOCHROMATIC_IMAGE/SMALL_IMAGE/EMPTY 전부 지원(사용자가 어떤 앱을 지정하든 렌더링되게), EMPTY일 땐 얇은 흰 테두리만 표시
- SLOT_MID_RIGHT: `defaultSystemProvider="APP_SHORTCUT"`(음성녹음), TOP 슬롯과 동일 패턴
- 빌드 성공. 에뮬레이터 재검증은 안 함(6단계에서 재부팅 후 재확인 예정이라 이번엔 생략, 13단계에서 4개 슬롯 한번에 확인)
- 4개 슬롯(1~4) 구현 완료 — 남은 건 SLOT_SUNSET/SLOT_SUNRISE(8단계)

## 8단계: 콤플리케이션 슬롯 5~6 (일몰/일출) - 2026-08-27
- `ComplicationSlot`(slotId 5=SLOT_SUNSET, 6=SLOT_SUNRISE) 추가, `defaultSystemProvider="SUNRISE_SUNSET"`, supportedTypes="SHORT_TEXT"
- 좌상단(x=115,y=52)/우상단(x=255,y=52), 70x40 사각 영역(`BoundingBox`), 라벨("일몰"/"일출" 고정 한글) + `[COMPLICATION.TEXT]` 값 2줄 구성, 흰색+다크아웃라인
- CLAUDE.md 7-1 "사용자 변경 허용 여부는 구현 단계에서 결정" 항목: `isCustomizable="false"`로 결정(라벨이 고정 텍스트라 다른 데이터소스로 바꾸면 라벨과 내용이 안 맞아 혼란 발생 가능)
- 빌드 성공. 콤플리케이션 슬롯 6개(1~6) 전부 구현 완료 — PROGRESS.md 4번 체크
## 콜드부팅 재검증 (2026-08-27) — 6단계 recovery 이슈 해소 확인
- 에뮬레이터 완전히 kill 후 `-wipe-data -no-snapshot-load`로 클린 콜드부팅, 재설치/재활성화
- **결론: recovery/세이프모드 재현 안 됨.** 로그에 "Forcing to fallback"/recovery 관련 라인 전혀 없음 — 지난번 6단계 이슈는 우리 XML 문제가 아니라 에뮬레이터 리소스 과부하(동시에 캘린더 앱 ANR 등)였던 것으로 결론
- 실제 디자인 정상 렌더 확인(배경/날짜/요일/메인시계/오전오후 세로/날씨아이콘/일몰일출 라벨 전부 정상)
- **새로 발견한 사항(버그 아님, 설계 갭)**: 콤플리케이션 슬롯 6개 중 SLOT_TOP_LEFT/TOP_RIGHT/MID_RIGHT(App Shortcut 3개)는 로그상 `NOT_CONFIGURED` 상태로 잡히는데, watchface.xml에 `<Complication type="NOT_CONFIGURED">` 렌더를 안 만들어놔서 첫 설치 시 화면에 아무것도 안 보임(다크 배경원조차 안 나옴) — App Shortcut은 시스템이 자동으로 값을 안 채워주고 사용자가 직접 앱 지정해야 채워지는 정상 동작이라 "버그"는 아니지만, 미지정 상태에서 UI가 완전히 비어보이는 건 사용자 경험상 아쉬움. SLOT_MID_LEFT(EMPTY)는 얇은 흰 테두리로 정상 표시됨(NOT_CONFIGURED가 아니라 EMPTY로 바로 잡혀서). SLOT_SUNSET/SUNRISE는 "일몰 --"/"일출 --"로 정상 표시(SUNRISE_SUNSET provider는 자동 배정, 데이터만 없음)
- 사용자에게 NOT_CONFIGURED 플레이스홀더 추가 여부 확인 필요(다음 지시 대기)

## NOT_CONFIGURED 플레이스홀더 추가 (2026-08-27)
- 1차 시도: `type="EMPTY"`로 추가했으나 재검증 결과 미표시 그대로 — 로그 확인해보니 App Shortcut 슬롯은 실제로 `EMPTY`가 아니라 `NOT_CONFIGURED`라는 별개의 런타임 타입으로 요청됨(공식 문서 요약엔 7종만 나열돼 있었지만 실기 로그로 반증됨)
- SLOT_TOP_LEFT/TOP_RIGHT/MID_RIGHT supportedTypes에 `NOT_CONFIGURED` 추가 + `<Complication type="NOT_CONFIGURED">`(다크 배경 원 + "+" 아이콘) 렌더 추가
- 에뮬레이터 재검증: 4개 슬롯(TOP_LEFT/TOP_RIGHT/MID_RIGHT는 다크원+"+", MID_LEFT는 기존 흰 테두리) 전부 정상 표시 확인. SLOT_SUNSET/SUNRISE도 여전히 정상("일몰 --"/"일출 --")
- 콤플리케이션 6개 슬롯 전부 미설정 상태에서도 정상 표시됨 — 확인 완료

## 9단계: 하단 알약1~2 (배터리/걸음수) - 2026-08-27
- 하단 알약1(배터리 x=93), 알약2(걸음수 x=179), 각 80x36, `RoundRectangle`(cornerRadiusX/Y=18, 완전 라운드) 배경 + `Text isAutoSize="true"`(Font minSize="10")로 텍스트 오토핏
- 배터리: `[BATTERY_PERCENT]` → "100%", 걸음수: `[STEP_COUNT]` → "0보"(한글 단위 붙임)
- 알약3(심박수+스트레스) 자리는 x=265에 비워둠(10단계)
- 빌드 성공, 에뮬레이터 검증 완료(정상 렌더, "100%"/"0보" 정상 표시 — 에뮬레이터라 걸음수는 0)
- 참고: 이번에도 에뮬레이터 프로세스가 중간에 한 번 죽어서 재부팅 후 재검증함(반복되는 환경 불안정, 우리 XML 문제 아닌 것으로 보임 — 계속 관찰 필요)

## 10단계: 하단 알약3 (심박수+스트레스) - 2026-08-27
- 알약3(x=265) 추가, `[HEART_RATE]` 사용, "0bpm" 형식(포맷은 알약1/2와 동일한 오토핏 라운드 알약 스타일)
- **스트레스 지수**: CLAUDE.md 7-2에 이미 "공식 SourceType 미지원 확인됨"으로 기록돼 있었고, 이번 조사(WFF SourceType 공식 목록 재확인)로도 STRESS 관련 SourceType 부재 재확인 → 심박수만 표시, 스트레스는 미구현으로 확정
- 이 자리엔 BODY_SENSORS류 권한 요청 다이얼로그가 뜰 수 있어 실기기에서 최초 실행 시 사용자 허용이 필요할 수 있음(에뮬레이터에서는 그런 프롬프트 없이 바로 0bpm 표시됨 — 센서 없어서 값만 0)
- 빌드/에뮬레이터 검증 완료(정상 렌더, "0bpm" 표시)
- PROGRESS.md 5번 체크(하단 데이터 바인딩 3종 전부 완료)

## 알약 3종 아이콘+값 구성으로 보강 (2026-08-27)
- CLAUDE.md 5번 15번 정정(심박수 알약도 배터리/걸음수처럼 아이콘+값 구성)에 맞춰 배터리·걸음수 알약에도 아이콘 추가(이전엔 텍스트만 있었음)
- 이미지 리소스 없이 PartDraw 기본 도형(Rectangle/Ellipse/RoundRectangle)으로 직접 그림: 배터리(사각 몸통+단자), 걸음수(점 2개=발자국 약식), 심박수(♥ 유니코드 글리프, #EF5350)
- 아이콘(좌) + 값 텍스트 오토핏(우) 가로 배치로 3알약 모두 통일
- 빌드/에뮬레이터 검증 완료 — 3알약 균형 있게 정상 렌더 확인

## 11단계: AOD 레이아웃 - 2026-08-27
- 배경/일몰일출/온도/날씨아이콘/콤플리케이션 6개/날짜/요일/오전오후/초/하단알약3종 전부 `<Variant mode="AMBIENT" target="alpha" value="0" />`로 숨김. 메인시계(시:분)만 유지
- 구현 방식: Condition류(배경/온도/날씨아이콘/요일/오전오후)는 `<Group x=0 y=0 width=438 height=438>`로 통째로 감싸서 Group에 Variant 적용(자식 좌표는 Scene과 동일 좌표계라 그대로 유지, 변환 불필요). ComplicationSlot/PartText/TimeText는 Variant를 직접 자식으로 추가(공식 지원 확인됨)
- 초(seconds) TimeText는 DigitalClock이 아니라 TimeText 자체에 Variant를 넣음 — 예전 아날로그 스켈레톤의 SecondHand Variant 위치와 동일 패턴(leaf 엘리먼트에 붙이는 게 맞는 것으로 확인)
- 에뮬레이터 검증: `adb shell input keyevent KEYCODE_SLEEP`(+`adb emu power ac off`)로 실제 AMBIENT 진입 확인 — 검은 배경에 "07:52"만 표시, 나머지 전부 숨김 정상 동작. `KEYCODE_WAKEUP`으로 인터랙티브 복귀도 정상(레이아웃 그대로 유지)
- PROGRESS.md 7번 체크

## 12단계: 폰트 확인 - 2026-08-27
- watchface.xml 전체 `<Font>` 32개 전수 확인 — 전부 `family="SYNC_TO_DEVICE" weight="BOLD"` 일관 적용됨(`<Text>` 30개 + `<TimeText>` 2개 = Font 32개, 누락 없음)
- 수정 필요 사항 없음

## 13단계: 빌드 + PC 에뮬레이터 테스트 - 2026-08-27
- `:watchface:assembleDebug` 빌드 성공, AVD `GalaxyWatch8_Test`(Wear OS 5)에 설치/활성화, 최종 스크린샷으로 전체 레이아웃(배경/온도/날씨아이콘/일몰일출/콤플리케이션6개/날짜요일/메인시계/오전오후/초/하단알약3종) 정상 렌더 재확인
- **날씨 데이터 로드 실패, 실기기에서 확인 필요** — CLAUDE.md 6-6 예상대로 에뮬레이터는 `LocationNotAvailableException`으로 위치를 못 가져와 `[WEATHER.*]` 전부 미수신 상태(온도 0°C, 아이콘 Default 폴백, 일몰/일출 "--"). XML 자체는 정상 동작 확인됐고, 실제 날씨 값 검증은 실기기(GPS 있는 환경)에서 필요
- AOD(11단계)도 같은 빌드로 재확인 완료(직전 세션에서 검증)
- PROGRESS.md 9번 체크

## 15단계: WFF Validator 검증 - 2026-08-27
- google/watchface GitHub 리포 소스에서 `wff-validator.jar` 직접 빌드(`third_party/wff` 서브프로젝트) 후 watchface.xml 실제 검증
- **실제 버그 6건 발견 및 수정**:
  1. `<Group>`에 필수 `name` 속성 누락 → 전체 26개 Group에 고유 name 부여
  2. boolean 속성값 소문자(`true`/`false`) → 대문자(`TRUE`/`FALSE`)로 수정(WFF는 대문자 enum 요구)
  3. `<Complication type="NOT_CONFIGURED">` + supportedTypes의 NOT_CONFIGURED → 공식 스키마에 없는 값 확인, 제거(App Shortcut 미설정 시 다시 빈 화면으로 — 6단계에서 만든 안내 아이콘 폴백은 비공식 동작이었음이 밝혀짐, 대신 EMPTY 타입 폴백만 유지)
  4. `<Image tintColor="...">` → Image는 tintColor 미지원, 부모 `<PartImage>`로 이동
  5. `<Stroke width="...">` → 올바른 속성명은 `thickness`
  6. `<TimeText>`의 `<Font>`는 자식 요소(Outline/Shadow) 전혀 미지원 확인 → 제거하고 대신 메인시계 뒤에 반투명 다크 배경(RoundRectangle, AOD에서 숨김) 추가해서 가독성 확보
- **버전 실측 이슈(가장 중요)**: validator가 "최대 지원 버전 #5"라길래 처음엔 v5로 올렸으나(사용자 승인), 실제 에뮬레이터(Wear OS 5)에 설치해보니 manifest `format.version`=5/4/3 전부 `FavoriteOperationException: Watch face package is not installed`로 설치 자체가 거부됨. version=2로 내리니 클린 재부팅 후 정상 설치·렌더 확인됨 → **v2로 최종 확정**(CLAUDE.md 1번, 3-1 갱신)
  - 부작용: v2 XSD 기준으로는 `isAutoSize`, `verticalAlign`(TimeText) 등이 "미지원"으로 계속 리포트됨 — 하지만 실제 런타임(manifest=2 선언 상태)에서 정상 렌더되는 걸 스크린샷으로 확인했으므로 XSD 쪽이 보수적/버전 불일치인 것으로 판단하고 그대로 유지하기로 결정(CLAUDE.md 3-1에 근거 기록)
- AndroidManifest.xml `com.google.wear.watchface.format.version`을 기존 "1"(초기 스캐폴드 값, 검토 안 됐던 값)에서 "2"로 정정
- 최종 상태: 빌드 성공, 클린 부팅 에뮬레이터에서 전체 요소 정상 렌더 재확인. WFF Validator는 v2 기준으로 2개 항목(isAutoSize, verticalAlign) 리포트하지만 실기기 검증 결과 우선하여 의도적으로 유지

## 알림점 + 워치페이스 이름 구현 (2026-08-27) — 5번 레이아웃 17개 항목 전부 구현 완료
- 최상단 중앙 알림점: `[UNREAD_NOTIFICATION_COUNT] > 0` Condition, 있으면 주황 점(14px) 표시, 없으면 alpha=0(자리는 있지만 안 보임)
- 최하단 워치페이스 이름: "바람개비 워치8 클래식" 고정 텍스트, 흰색+다크아웃라인
- 둘 다 AOD에선 숨김(5-1)
- 빌드 성공, WFF Validator v2 재검증 — 기존 known gap(verticalAlign) 외 신규 에러 없음
- 에뮬레이터 완전 초기화(`-wipe-data` 콜드부팅) 후 재설치·재검증 완료 — 전체 레이아웃 정상 렌더 확인
- **CLAUDE.md 5번 레이아웃 스펙 1~17번 전 항목 구현 완료**

## 16단계: 레이아웃/색상 전면 재조정 (2026-08-28)
- 사전 확인: CLAUDE.md/docs/weather_icons.md/docs/개발_지시서.md/docs/demo_final.png 최신본 정합 확인 후 진행
- 온도: 색상 #FFA726→#26C6DA(터콰이즈), 2행 구조로 분리(1행 온도값/2행 날씨코멘트), 일출·일몰 라벨/값 행(52-68/68-92)과 정확히 같은 높이로 정렬
- 날씨 코멘트: WEATHER.CONDITION 0~15 전부 매핑(docs/weather_icons.md 코멘트표 그대로) 신규 구현, 온도 바로 아래 표시
- 일몰 라벨 색상 #FFFFFF→#C97B5F(테라코타), 일출 라벨 색상 #FFFFFF→#FFB199(새벽톤) — 값 텍스트는 공통 흰색 유지. 위치(좌/우) 자체는 손대지 않음(18단계에서 함께 처리 예정, 아래 메모 참조)
- 오전/오후, 초(seconds) 색상 통일: #FFA726/#4FC3F7 → 코럴톤 #FFB27A 하나로
- 날짜 색상 #FFFFFF→#FFDDAA(골드), 날짜/요일 seam 좌표 222→219로 미세 조정(캔버스 중앙 219 기준 정확히 대칭)
- 콤플리케이션 6~9번(상단좌/우, 중단좌/우) 4개 크기를 64px/60px 혼재 → 62px로 통일(중심 좌표 유지, 내부 아이콘/EMPTY "+" 크기도 비례 조정)
- 하단 알약3개/일출일몰 슬롯 위치·잠금/날씨아이콘 조건분기/워치이름 곡선은 지시대로 미변경(각 17~20단계 예정)
- 빌드(`assembleDebug`) 성공, 에뮬레이터(`GalaxyWatch8_Test`) 콜드 설치·활성화 후 스크린샷으로 재검증 완료(터콰이즈 온도/코멘트/코럴 오전·초/골드 날짜/콤플리케이션 4개 통일 크기 전부 정상 렌더)
- **다음 세션 확인 필요**: CLAUDE.md 5-2/5-4에 "좌측=일출, 우측=일몰"로 자리 정정이 이미 확정돼 있으나, 코드는 아직 기존 자리(좌=일몰 SLOT_SUNSET, 우=일출 SLOT_SUNRISE) 그대로임 — 16단계 지시서상 "일출일몰 슬롯"은 손대지 말라고 되어있어 위치 교체는 보류함. 18단계(잠금 속성 추가) 진행 시 함께 스왑할지 사용자 확인 필요

## 17단계: 하단 알약 3개 구조 개편 (2026-08-28)
- 개발_지시서.md 갱신 확인(18단계에 "좌우 위치 교체" 항목 추가된 것 확인) 후 17단계 진행
- 독립 타원 3개, 타이틀("워치 배터리"/"걸음 수"/"심박수")+아이콘(좌측 고정)+값(우측 정렬) 2행 구조로 전면 개편
- 폭 차등 적용: 배터리 96px(중, x=63) / 걸음수 124px(대, x=167) / 심박수 76px(소, x=299), 가운데 정렬(좌우 여백 63px 대칭)
- 배경 rgba(35,26,52,.8)=#CC231A34 + 테두리 rgba(255,255,255,.22) 1px=#38FFFFFF, 텍스트/아이콘 밝은 민트 #C9F7DC(심박 아이콘만 기존 레드 #EF5350 유지)
- 걸음수 값 폰트: [STEP_COUNT] 자릿수 구간별(< 100/1000/10000/이상) Condition 분기로 20/17/15/12 명시적 크기 적용(양방향 오토핏 — WFF isAutoSize는 축소만 지원해 확대는 자체 Condition 분기로 구현), 최상위 구간은 isAutoSize 보조 적용
- 배터리/심박수는 폰트 크기 고정(14), 단위는 배터리만 %(demo_final.png 기준 걸음수/심박수는 단위 없이 숫자만)
- 빌드(`assembleDebug`) 성공, 에뮬레이터 재설치·활성화 후 스크린샷 재검증 완료(3알약 폭 차등/좌우 정렬/민트톤 정상)

## 18단계: 일출/일몰 슬롯 잠금 + 좌우 위치 교체 (2026-08-28)
- SLOT_SUNRISE(일출)를 x=115(좌상단)로, SLOT_SUNSET(일몰)을 x=255(우상단)로 위치 교체(CLAUDE.md 5-2/5-4, 7-1 자리 정정 확정사항 반영). slotId(5=SUNSET/6=SUNRISE)는 그대로 유지, 위치만 스왑
- isCustomizable="FALSE"는 8단계에서 이미 적용돼 있던 상태 재확인(변경 불필요)
- 빌드 성공, 에뮬레이터 재설치·활성화 후 스크린샷 재검증 완료(좌="일출", 우="일몰" 정상 표시)

## 19단계: 날씨 아이콘 매핑 갱신 (2026-08-28)
- docs/weather_icons.md 새 매핑표(32종 1:1 확정, 예비/병합 없음) 기준으로 4단계에서 만든 조건분기를 전면 재작성
- 기존엔 c_clear(1|8 병합)/c_light_snow(10|11 병합)/Default(0,2,3,13 병합) 등 유사 이미지끼리 OR로 묶여있었는데, 새 매핑표는 16개 코드 전부 서로 다른 이미지라 병합 없이 코드별(c1~c15 + Default=0) 완전 개별 Compare로 분리
- 신규로 쓰이게 된 이미지(day_04_clear_sun_b/night_04_stars_clear, day_14_cloud_dark_angry/night_14_cloud_angry, day_03_cloud_double/night_03_cloudy_plain, day_12_wind_strong/night_12_wind_strong, day_15_rain_beanie_happy/night_15_rain_beanie_happy) 좌표는 실제 webp 픽셀 크기(RIFF/VP8L 헤더 직접 파싱으로 확인, 전부 최대변 130px 리사이즈 상태)를 기준으로 기존 방식과 동일하게 중앙 앵커(x=219,y=158) 기준 계산
- **docs/weather_icons.md 오탈자 확인**: 문서 파일명 목록엔 "night_08_snow_beanie_happy"로 적혀있으나 실제 drawable 리소스 파일명은 "night_08_snow_beanie_angry"임(기존 코드가 이미 올바른 실제 파일명을 쓰고 있었음, 버그 아니었음) — 문서만 표기 오류, 코드 변경 불필요
- 빌드 성공, 에뮬레이터 재설치·활성화 후 스크린샷 검증: WEATHER 데이터 미수신(에뮬레이터 한계, 6-6) 상태의 Default(코드0=UNKNOWN) 아이콘이 기존 cloudy_plain에서 새 매핑대로 rain_beanie_happy로 정상 전환된 것 확인. 실제 CONDITION 값별 분기는 실기기(위치 데이터 있는 환경)에서 재확인 필요

## 20단계: 워치이름 곡선 텍스트 (2026-08-28)
- **기술 검증 결과**: WFF 공식 레퍼런스(`TextCircular` 엘리먼트) 확인 — 원호를 따라 글자 단위로 휘는 곡선 텍스트가 공식 스키마에 존재하긴 하나 **"Introduced in Wear OS 4"(WFF v4+ 전용)**로 명시됨. 본 프로젝트는 15단계에서 v3~v5 전부 실기기/에뮬레이터 설치 거부(FavoriteOperationException)로 v2 확정된 상태라 TextCircular 사용 불가 확인
- CLAUDE.md 5-16 대안 중 **②(곡선 포기, 직선 텍스트 유지)** 채택 — 대안①(전체 회전)은 하단 중앙 대칭 텍스트에 일괄 rotate를 걸면 한쪽으로 기울어져 보여 의도한 "정수리(가운데 볼록)" 형태를 오히려 왜곡시키므로 제외
- 곡선 적용은 보류하되, 6-1/5-16에 명시된 나머지 스타일(피치/탠 `#F2C9A0` 색상, 볼드 아님→`weight="NORMAL"`)은 이번에 반영(기존엔 흰색+BOLD로 남아있던 부분, 16단계에서 누락됐던 항목)
- 빌드 성공, 에뮬레이터 재설치·활성화 후 스크린샷 검증 완료(피치톤 비볼드 텍스트 정상 표시)
- **CLAUDE.md 5-16 항목에 이 결론(v2 환경에서 TextCircular 불가, 직선 유지로 최종 확정) 반영 필요 — 다음 CLAUDE.md 정리 시 갱신 권장**

## 21단계: 화면 검수 버그 수정 (2026-08-28, wipe-data 재검증 캡처 기반)
1. **메인시계 배경박스 제거**: TimeText/Font는 Outline/Shadow 자식을 지원하지 않는 것이 공식 문서(WFF TimeText reference, Wear OS 4+)로도 재확인됨. 15단계 때 임시로 반투명 배경박스로 대체했던 것을 이번에 제거하고, 대신 검정(#B3000000) TimeText 7개를 ±2px 8방향으로 겹쳐 그리는 페이크 스트로크로 교체 — 배경 없이 흰색+다크아웃라인 효과 재현. AOD(순수 검정 배경)에서도 검정 텍스트가 묻혀 시각적 차이 없음 확인
2. **요일 색상**: 스크린샷 픽셀을 직접 샘플링해 확인한 결과 금요일 글자가 정확히 `#F06292`로 렌더링되고 있음(오차 0) — 실제로는 정상이며, 배경 그라디언트 톤과 색상이 근접해 육안上 흐려 보인 것으로 판단(버그 아님, 수정 안 함)
3. **콤플리케이션 4개 미설정 플레이스홀더**: 기존엔 `<Complication type="EMPTY">`에만 원+"+" 렌더를 넣어뒀는데, 실제 에뮬레이터 확인 결과 App Shortcut 3개(TOP_LEFT/TOP_RIGHT/MID_RIGHT)는 미설정 시 시스템이 EMPTY 타입 자체를 요청하지 않아(어떤 Complication type 분기도 매칭 안 됨) 화면에 아무것도 안 그려지는 실제 버그였음(MID_LEFT도 "+" 없이 얇은 테두리만). CLAUDE.md 6~9번 사양대로 4개 슬롯 전부 ComplicationSlot과 무관한 별도 Group(`group_placeholder`)으로 항상 원+"+"를 먼저 그려두고, 그 위에 ComplicationSlot이 렌더되는 구조로 변경 — 실제 콤플리케이션 콘텐츠가 있으면 그 위에 덮여 보임
4. **날씨 데이터 0°C/정보없음/-- 관련**: CLAUDE.md 6-6에서 말하는 "날씨 데이터 로드 실패" 상황이 맞음 — WEATHER.CONDITION이 실제로 0(UNKNOWN)을 수신한 게 아니라, 에뮬레이터가 위치 정보를 못 가져와(LocationNotAvailableException, 13단계에서 이미 확인됨) WEATHER.* 계열 전체가 데이터 자체를 못 받는 상태. 이때 각 표현식이 각자의 fallback(온도 0, CONDITION 미매칭→Default 분기, COMPLICATION.TEXT 미수신→"--")을 보여주는 것뿐이며, 화면에 별도의 "로드 실패" 안내 UI가 뜨는 게 아님. 실제 CONDITION 값 검증은 실기기(위치 있는 환경)에서 확인 필요
- 빌드 성공, `emulator -wipe-data` 콜드부팅으로 완전 초기화 후 재설치·재검증(인터랙티브+AOD 둘 다 스크린샷 확인) — 4가지 항목 전부 정상 반영 확인

## 21-1단계: 좌표/폰트/아이콘 정밀 정합 (2026-08-28, demo_final.png 재대조)
- CLAUDE.md 5-2 정확 좌표 스펙표(demo.html 실측값) 기준으로 전체 요소 x/y/width/height 임의 조정 없이 표 숫자 그대로 재적용:
  알림점(214,8,10x10) / 일출·일몰 라벨+값 슬롯(71·285,58,82x57, 내부 라벨26+gap5+값26) / 온도값(167,55,104x31)·코멘트(167,93,104x18) /
  날씨아이콘(전체 영역 0,121,438x105 → 세로중심 173.5 기준 32종 전부 y좌표 재계산) / 콤플리케이션 4개(52x52, 내부 아이콘/+ 크기도 비례 축소) /
  날짜·요일행(y224,h32) / 오전오후(78,264,32x59, 폰트 20로 축소) / 메인시계(116,254,190x78, 폰트 50→56, 8방향 아웃라인 오프셋도 함께 이동) /
  초(311,274,49x50, 폰트 22) / 알약3종(y340,h44, 배터리x65·걸음수x167·심박수x297) / 워치이름(139,395,160x32)
- **아이콘 이슈 발견 및 대응**: demo_final.png와 동일하게 컬러 이모지(🔋👣❤️)를 시도했으나, 스크린샷 픽셀 확인 결과 WFF `<Text>` 렌더러가 이모지를 컬러가 아닌 폰트 지정색 단색(흰색)으로만 그리는 것 확인(WFF 텍스트 렌더링 경로가 컬러/COLR 이모지 폰트 미지원으로 추정) → **못 하는 부분**: 이모지 기반 완전 컬러 아이콘은 WFF에서 구현 불가 확인. 대신 기존 방식대로 벡터 도형(PartDraw)에 색상을 입혀 대체(배터리=그린 `#66BB6A`, 걸음수=블루 `#29B6F6`, 심박수=레드 `#EF5350` 하트 글리프) — 완전한 emoji 아트와는 다르지만 컬러 구분은 확보
- **폰트 이슈 확인**: 전체 Font 요소는 여전히 `family="SYNC_TO_DEVICE" weight="BOLD"` 일관 적용 확인됨(12단계 감사 결과와 동일, 코드상 누락 없음). "기본 시스템 폰트로 보임" 현상은 AOSP 기반 Wear 에뮬레이터의 기본 시스템 폰트가 실기기(갤럭시 워치8, Samsung One UI Watch의 Noto Sans KR Bold)와 다를 가능성이 높음 — SYNC_TO_DEVICE는 "기기 시스템 폰트를 그대로 따른다"는 의미라 에뮬레이터에서는 실기기와 다른 폰트로 보일 수 있음(코드 문제 아님, **실기기에서만 최종 확인 가능**)
- 빌드 성공, 에뮬레이터 재설치·활성화 후 인터랙티브+AOD 스크린샷 재검증 완료 — 전체 레이아웃이 demo_final.png와 비율/배치상 크게 근접

## 폰트 임베딩 + 알약 아이콘 이미지화 (2026-08-28, 사용자 재지적 반영)
- **CLAUDE.md 6-2 정정 확인**: 삼성 워치 시스템 기본폰트는 Noto Sans KR이 아니라 SamsungOne — `SYNC_TO_DEVICE`로는 Noto Sans KR이 절대 안 나옴을 사용자가 확인해줌
- Google Fonts에서 Noto Sans KR Bold/Regular 두 웨이트 TTF(SIL OFL, v39, korean+latin 서브셋)를 받아 `watchface/src/main/res/font/notosanskr_bold.ttf`, `notosanskr_regular.ttf`로 추가(각 2.4MB)
- watchface.xml 전체 `family="SYNC_TO_DEVICE"` 65곳 → `notosanskr_bold`(64곳, weight=BOLD) / `notosanskr_regular`(워치이름 1곳, weight=NORMAL)로 교체, `SYNC_TO_DEVICE` 완전 제거
- 공식 문서 확인: `res/font/{name}.ttf` 배치 후 `family="{name}"`(확장자 제외)로 참조하면 커스텀 폰트 임베딩됨(TimeText/PartText 공통 지원)
- **아이콘 이미지화**: 이모지 컬러 렌더링 불가 확인(직전 세션) → 벡터 도형 대신 날씨아이콘과 동일한 방식으로 실제 컬러 PNG 아이콘 제작
  - PowerShell System.Drawing으로 직접 드로잉: `icon_battery.png`(그린,36x24), `icon_steps.png`(블루 발자국 2개,28x44), `icon_heart.png`(레드 하트,32x32) — 전부 투명배경, `watchface/src/main/res/drawable/`에 추가
  - WebP 대신 PNG 사용(3-4 규정은 GIF 대체용 WebP 권장이 취지이며, 이런 초소형 단순 아이콘은 PNG도 무손실+용량 미미해 문제 없음)
  - watchface.xml 알약 3종 아이콘을 `<PartImage><Image resource="icon_*"/></PartImage>`로 교체(기존 PartDraw 벡터/이모지 텍스트 제거)
- 빌드 성공, 에뮬레이터 재설치·활성화 후 인터랙티브+AOD 스크린샷 재검증 완료 — 컬러 아이콘 3종 정상 렌더, 커스텀 폰트로 시:분/전체 텍스트 정상 표시(글리프 깨짐 없음)
- **후속 확인 필요**: 폰트 파일 추가로 패키지 용량이 약 4.8MB 증가함 — 15단계 WFF Validator로 메모리 재검증 권장(다음 세션)

## 폰트 크기 재조정 (2026-08-28, 커스텀 폰트 em-box 축소 보정)
- 사용자 실측 결과 반영: 메인시계 흰 글자 폭 143px(스펙190 기준 75%)/날짜 106px/워치이름 104px(스펙160 기준 65%) — SYNC_TO_DEVICE→커스텀 Noto Sans KR TTF 교체(21-1단계) 후 동일 font-size에서도 폰트 자체 em-box가 작아 렌더 크기가 축소되는 현상 확인
- 메인시계 실측 폭 143→목표190 비율(×1.329)을 전체 폰트 크기의 공통 배율로 채택해 XML 내 모든 `size` 값에 일괄 적용(치환 충돌 방지를 위해 임시 토큰 2단계 치환 방식 사용):
  9→12, 12→16, 13→17, 14→19, 15→20, 16→21, 18→24, 20→27, 21→28, 22→29, 24→32, 56→74
  (일출/일몰 라벨 12→16, 값 16→21, 온도 24→32, 날씨코멘트 13→17, 날짜/요일/오전오후 20→27, 메인시계 56→74, 초 22→29, 알약 타이틀 9→12·값 14→19, 걸음수 오토핏 21/18/16/13→28/24/21/17, 워치이름 12→16, 콤플리케이션 "+"/폰배터리 숫자 21·15→28·20 등 전 항목 포함)
- 빌드 성공, 에뮬레이터 재설치·활성화 후 재검증. 스크린샷 픽셀 스캔으로 실측 재확인:
  메인시계 폭 143→**189px**(스펙190 대비 99.5%), 날짜 폭 106→**145px**(스펙160 대비 91%), 워치이름 폭 104→**153px**(스펙160 대비 96%) — 전부 목표 근접
  일출/일몰 라벨·온도·날씨코멘트·알약 3종·콤플리케이션 "+"/폰배터리 숫자 등 나머지 요소도 각 박스 폭 대비 오버플로 없이 정상 표시 확인(크롭 캡처로 육안 검증)
- AOD 재확인 완료(시:분만 정상, 확대된 크기로도 검정 배경 안에 문제없이 표시)

## 날짜행 위치 + 날씨아이콘/코멘트 겹침 수정 (2026-08-28)
- **날짜행 y=224 처짐 문제**: XML에는 이미 y="224"로 정확히 들어있었으나(코드 오타 아님), verticalAlign="CENTER"가 커스텀 폰트(Noto Sans KR)의 큰 line-height(ascent/descent) 기준으로 중앙정렬을 계산해서 실제 글자가 박스 하단쪽으로 치우쳐 렌더링되는 폰트 metrics 이슈로 확인(픽셀 스캔: box는 y224~256인데 실제 글자는 y242~259에 렌더링, 시각적 중심이 기하 중심보다 +10.5px 아래). 실측 기반으로 XML y값을 224→**209**로 보정해 시각적 위치를 스펙 의도(y≈224 부근)에 맞춤(날짜/요일 둘 다 적용)
- **날씨 아이콘 32종 상단 여백 확인(요청 1)**: 2단계에서 전 이미지 "투명 여백 제거" 트림을 거쳤기 때문에, 각 webp의 실제 픽셀 크기 자체가 이미 콘텐츠 딱 맞는 바운딩박스임(내부에 숨은 여백이 따로 없음) — day_15/night_15(비니 캐릭터)만 유독 여백이 적은 게 아니라, **높이가 130에 가까운 이미지 전부(c1,c5,c6,c7,c8,c9,Default 등) 동일하게 위쪽 끝까지 꽉 차 있음**을 확인. day_15가 두드러져 보인 건 마침 CONDITION 미수신 상태의 기본 폴백이라 화면에 계속 떠 있었기 때문이지 그 이미지 자체만의 결함은 아님
- **날씨코멘트~아이콘~날짜 간격 문제(요청 2)**: 코멘트 하단(y=111 고정)과 날짜 상단(보정 후 실측 y≈226) 사이 실제 가용 공간이 115px뿐인데, 아이콘 최대변이 130px라 이 공간에 넣으면 양쪽 중 한쪽은 반드시 침범하는 구조적 문제였음(단순 y 이동만으론 한쪽 겹침이 다른 쪽으로 옮겨갈 뿐 — 실제로 1차 시도(y+10 이동)에서 코멘트와는 여유 생겼지만 날짜와 새로 겹치는 것 확인). 최종적으로 **아이콘 최대변을 130→100px로 축소**(32종 전부 비율 유지, 중심앵커 (219,168.5)로 재배치)해 위아래 각 7~8px 여유 확보
- 빌드 성공, 에뮬레이터 재설치·활성화 후 재검증 — 코멘트/아이콘/날짜 3구간 모두 겹침 없이 정상 간격 확인(근접 확대 캡처로 육안 검증)

## 데모 픽셀대조 확정버그 2건 수정 (2026-08-28)
- 날짜행(y=224)은 오차 2px 이내로 정상 확인되어 미변경(사용자 실측 결과 반영)
- **1. 초(seconds) 크기/정렬**:
  - 1차로 font-size만 29→39로 올렸으나 실측 잉크높이가 30→31로 거의 그대로였음 → box height=50(y274~324)이 커진 폰트의 ascent를 위에서 잘라내는 클리핑으로 확인
  - box를 y254~324(height70, 메인시계와 동일 y254 시작)로 확장해 클리핑 여유 확보 후 size=50으로 재조정, verticalAlign을 CENTER→**BOTTOM**으로 변경(메인시계 하단 기준 정렬)
  - 재실측: 잉크높이 **40px**(목표 40px 정확히 달성), 하단 y323(메인시계 하단 y332과 근접), 가로는 폭49 박스를 살짝(약12px) 넘지만 우측에 다른 요소 없어 시각적 문제 없음
- **2. 날씨 아이콘 박스 이탈**:
  - CLAUDE.md 5-2 스펙 박스(y121~226, h105) 기준으로 재계산 — 기존 최대변130px(이전 세션에서 100px로 축소했던 것도 박스 기준(121~226)이 아니라 코멘트/날짜 실측 기준 임시 앵커(168.5)로 잡아 스펙 박스와 어긋나 있었음
  - 최대변 130→**105px**로 축소(축척 105/130), 박스 정중앙 y=173.5 기준 32종 전부 x/y/width/height 재계산해 정확히 박스(121~226) 안에 들어가도록 재배치
  - 재검증(스크린샷): 코멘트("정보 없음")~아이콘 상단, 아이콘 하단~날짜 사이 모두 여유 있게 확인, 박스 이탈 없음
- 빌드 성공, 에뮬레이터 재설치·활성화 후 인터랙티브+AOD 재검증 완료

## 날짜행 중앙정렬 + 초 크기 재확인 (2026-08-28)
- **초(seconds) "미반영" 의혹 확인**: watchface.xml을 직접 열어 확인한 결과 직전 세션에서 적용한 값(x311,y254,w49,h70,size50,verticalAlign=BOTTOM)이 그대로 정확히 들어있었음(코드 누락 아님) → `adb uninstall` 후 완전 재설치(Favorite Id 9→10으로 실제 신규 등록 확인, 캐시/스테일 빌드 가능성 배제)해서 재측정한 결과 잉크높이 **40px로 스펙과 정확히 일치** — 실제로는 이미 정상 반영돼 있었고, 이전 측정 환경(에뮬레이터 화면 미갱신 등) 쪽 문제였던 것으로 판단
- **날짜행 좌우 치우침 수정**: 기존엔 날짜를 END정렬로 x=219에서 끝나고 요일을 START정렬로 x=219부터 시작하는 "seam" 방식이었는데, 날짜(10자)와 요일(1자)의 폭 차이 때문에 결합 블록의 실제 시각적 중심이 캔버스 중앙(219)보다 한참 왼쪽(실측 163.5)에 위치해 SLOT_MID_LEFT 원형(x70~122)과 겹치는 문제였음
  - 결합 텍스트 실측 폭(x77~250)을 기준으로 필요한 이동량(+56px)을 계산해 날짜 PartText(x59→**115**)와 요일 PartText 7개(x219→**275**) 전부 동일하게 우측 이동, 상대 간격(seam)은 그대로 유지한 채 블록 전체만 평행이동
  - 재검증: 결합 블록 중심 실측 x=222(목표219 대비 오차 3px), 좌측 SLOT_MID_LEFT(우측끝122)와 13px 간격, 우측 SLOT_MID_RIGHT(좌측끝316)와 7px 간격 확보 — 양쪽 모두 겹침 없음 확인(데모의 23~38px 여유보다는 좁지만 겹침은 완전히 해소)
- 빌드 성공, `adb uninstall`→재설치로 캐시 배제한 완전 새 설치 후 인터랙티브+AOD 재검증 완료

## 날짜행 미세정렬 + 날씨아이콘 축소 (2026-08-28)
- **날짜행 미세 우측이동**: 픽셀 스캔 기반으로 위치를 여러 차례 보정(+4px→과다 보정으로 우측 여백 3px까지 좁혀짐 확인 후 -7px 재보정)한 끝에 최종 x값(날짜=112, 요일=272)으로 확정. 재측정 결과 결합 블록 중심 x=218.5(목표219, 오차 0.5px), 좌측여백10px/우측여백11px로 거의 완전 대칭 확인
- **날씨 아이콘 "스펙 초과(118px)" 재확인**: watchface.xml 값 자체는(이미 21-2에서 최대변105px로 정확히 적용돼 있었음) 정상이었고, 픽셀 단위로 직접 재검증한 결과 실제 렌더도 스펙 박스(y121~226) 안에 들어가 있어 코드 결함은 없었음(이전 측정이 부정확했던 것으로 판단, 초 이슈와 유사 패턴)
- 다만 다크 아웃라인 halo 때문에 코멘트/날짜와 시각적으로 거의 붙어 보이는 문제는 실재해서, 사용자 지시대로 날짜 위치는 그대로 두고 **아이콘만 추가 축소(최대변 105→85px)** 해 위아래 여유를 더 확보(박스 중앙 y=173.5 기준 32종 재계산)
- 빌드 성공, 매 수정마다 `adb uninstall`로 완전 삭제 후 재설치(Favorite Id 11→12→13 순차 증가로 매번 진짜 새 설치임을 확인)하며 반복 재검증, 최종 스크린샷에서 날짜행 좌우 대칭 및 아이콘-코멘트/아이콘-날짜 간 여유 간격 육안 확인 완료. AOD 재확인 완료

## 날짜행 재보정 + 폰트 확대 시도 (2026-08-28)
- 날씨아이콘 축소/초 크기는 정상 확인됨(추가 조치 없음)
- **날짜행 방향 재확인**: "우측 이동"을 요청받은 대로 재적용(date x112→119, 요일 x272→279)해 위치 자체는 바로잡음
- **폰트 확대(+20%, size27→32) 시도 후 발견한 구조적 한계**: 사이즈를 키우자 텍스트 폭이 174→194px로 늘어났는데, 좌우 콤플리케이션 사이 가용폭이 정확히 194px(x122~316)뿐이라 **여백이 0에 가까워지며 양쪽 다시 살짝 겹침** 발생. 즉 "날짜폰트 20% 확대"와 "좌우 슬롯 비침범"은 현재 지오메트리(고정된 콤플리케이션 위치)에서 동시에 만족 불가능한 조건임을 확인
- 절충안 적용: size27→**28**(약 +3.7%, 완전한 20%에는 못 미침)로 최소 확대하고 위치 재중앙정렬(date x=116, 요일 x=276) — 재측정 결과 폭183px, 중심x=219(정확), 좌우 각 6px로 대칭 확보(겹침 없음, 다만 여유폭은 이전 13px보다 줄어듦)
- 빌드/재설치(Favorite Id 14→17 순차 증가로 매 회 새 설치 확인) 반복하며 최종 수렴, 인터랙티브+AOD 재검증 완료
- **사용자 확인 필요**: 데모와 동일한 24px급(20% 확대) 완전 매칭은 좌우 슬롯 겹침 없이는 불가능한 구조적 제약 — 현재는 "안전 마진 확보"를 우선해 3.7% 확대로 타협함. 폰트 확대를 더 우선하고 싶다면 좌우 슬롯 위치/크기 조정이 함께 필요함(별도 지시 필요)

## 날짜행 seam 좌표 계산값으로 교체 (2026-08-28)
- 반복된 픽셀 단위 미세조정("몇 px 옮겨줘")이 계속 어긋난 근본 원인 규명: 날짜(align=END)/요일(align=START) 두 PartText가 seam 좌표에서 맞닿는 구조인데, 그 seam 값(276)이 실측 계산이 아니라 이전 세션들에서 누적된 수동 추정값이었음(요청에 따라 코드 수정 없이 원문 값·구조만 먼저 보고함)
- 사용자가 잉크 폭 실측(날짜152px+간격5px+요일25px=182px) 기반으로 계산한 값을 그대로 적용, 추가 미세조정 없이 확정:
  - 날짜 PartText: x=116→**120**(width160·align=END 유지, ink 우측끝 x=280)
  - 요일 PartText 7개 전부(Group name="group_22" 자식): x=276→**285**(width40·align=START 유지, ink 좌측끝 x=285) — 7개 색상 분기 전부 동일 x로 통일
- 빌드 성공, `adb uninstall` 완전 재설치(Favorite Id 18로 신규 등록 확인) 후 인터랙티브+AOD 재검증 완료, 스크린샷상 날짜행 좌우 콤플리케이션과 대칭적인 여백으로 정상 표시 확인

## 날씨아이콘 재확대 (2026-08-28, 날짜행은 손대지 않음)
- 날짜/요일 seam(21-6) 확인만 하고 미변경
- 1차 시도: 최대변 85→90px로 확대 후 실측 → 아이콘 하단(y225)이 날짜행 ink 상단(y224)과 겹침(간격 -1px), 5px 최소 여유 조건 위반 확인
- 2차: 최대변을 78px로 하향해 재계산(원본 최대130px 기준 축척 0.6, 박스 중앙 y=173.5 기준 32종 재배치) → 재측정 결과 간격 정확히 **5px**(최소 조건 충족), 데모 목표치(88px)에는 못 미치지만 이전(85px)보다 커짐
- 빌드 성공, `adb uninstall` 완전 재설치(Favorite Id 19→20) 후 재검증, 인터랙티브+AOD 스크린샷 확인 완료

## 날씨아이콘 32종 height 전수 점검 (2026-08-28, 조회만·수정 없음)
- 현재 watchface.xml의 PartImage width/height 32종 전수 조회 완료(표는 대화 로그 참조)
- height 최댓값=**78px**(day: c1,c5,c6,c7,c8,c9,Default 7개 / night: c1,c5,c7 3개, 총 10개 항목 공동 최댓값), 나머지 22개는 52~76px로 더 작음
- 최댓값(78) 기준 이론적 박스 하단은 y=213이지만, 직전 세션에 동일 78px 구성(day_15 Default 폴백)으로 실측した 결과 실제 렌더 하단은 안티앨리어싱 번짐 포함 y≈219, 날짜행 ink 상단(y=224)과의 간격이 정확히 **5px**로 이미 확인된 상태
- 결론: height=78 항목들이 이미 최소 기준(5px)에 정확히 걸쳐 있을 뿐 위반은 아니며, 나머지 22개는 78보다 작아 간격이 더 넉넉함 → **개별 상한 적용 대상 없음, 수정 안 함**

## 날씨아이콘 강제실측 검증 + 고정박스 방식 전환 (2026-08-28)
- **직전 "height=78 → 5px 여유" 결론 검증**: 이론 계산이 아니라 실제로 확인 — Default 폴백을 임시로 day_01_clear_sun(h=78 그룹 소속, 원 모양이라 beanie 캐릭터와 형태가 전혀 다름)으로 바꿔 에뮬레이터에 띄우고 정밀 픽셀 스캔(행 단위 색상 존재 여부 이진 판정)으로 재측정
  - 날짜행 ink 상단 = **y224**(정확, 이전 결론과 일치, 사용자가 의심한 217~218은 불일치 확인)
  - 태양 아이콘 ink 하단 = **y219**
  - 실측 간격 = **정확히 5px** → 직전 결론(계산치) 그대로 실측으로 재확인됨, 오류 아니었음
  - 검증 후 임시 교체분은 원래 day_15_rain_beanie_happy로 즉시 원복
- **방식 전환(사용자 요청)**: 이미지마다 개별 width/height를 손으로 계산해 넣는 기존 방식 대신, 고정 박스(x=0,y=121,width=438,height=90) 기준 통일 규칙으로 전환
  - **WFF 자체 한계 확인**: 공식 문서(Image/PartImage 레퍼런스) 확인 결과 `resize`/`scaleType`/`object-fit` 류의 자동 비율맞춤 속성이 전혀 없음(Image는 `resource` 속성 하나뿐) → "이미지별 계산 없이 자동으로 박스에 맞추는" 기능은 WFF에 존재하지 않아 완전한 자동화는 불가능. 대신 32종 전부 **동일한 한 가지 공식**(height=90 고정, width=원본가로×(90/원본세로), x=219-width/2로 수평 중앙정렬)을 기계적으로 적용해 "개별 판단 없이 같은 규칙"이라는 취지는 충족
  - 결과: 세로는 32종 전부 정확히 90px로 통일(박스에 꽉 참), 가로만 원본 비율에 따라 68~136px로 자연스럽게 상이 — 박스(0,121,438,90) 밖으로 나가는 이미지 없음을 공식으로 보장
- **검증 캡처(서로 다른 4개 비교)**: day_01_clear_sun(원형,89×90) / day_10_cloudy_plain(광폭,136×90) / day_13_rain_crying(중간폭,106×90) / day_15_rain_beanie_happy(협폭,68×90, 최종 운영값) — 4종 전부 박스 안에 꽉 차게 들어가고 날짜행과 겹치지 않음을 스크린샷으로 확인
- 빌드/재설치 반복(Favorite Id 23→27) 후 최종 원복 상태로 인터랙티브+AOD 재검증 완료

## 날짜-메인시계 간격 6px로 축소 (2026-08-28)
- 지시대로 메인시계(9개 DigitalClock: 아웃라인용 8개+실제 1개, y=252/254/256 조합) / 오전오후(Group, y=264) / 초(DigitalClock, y=254)를 전부 -27px 일괄 이동
  - 메인시계: y=252→225, y=254→227, y=256→229 (width/height 등 나머지 속성 불변)
  - 오전/오후: y=264→237
  - 초: 지시서상 원래 y=274 가정이었으나 실제 파일값은 21-2단계 이후 y=254로 이미 변경돼 있던 상태 확인 → 254 기준으로 동일하게 -27 적용해 y=227(메인시계와 같은 값, 세트 정렬 유지)
- 날짜행(y=209)은 미변경
- 빌드/재설치(Favorite Id 28) 후 실측: 날짜 ink 하단 y=248, 메인시계 ink 상단 y=253 → 간격 **5px**(목표 6px 대비 1px 오차, 측정 해상도 범위 내 근접)
- 인터랙티브+AOD 스크린샷 재검증 완료, 오전/오후·초·메인시계 세트 정렬 어긋남 없음 확인

## 21-9 되돌리고 날짜행만 하향 이동 (2026-08-28)
- 메인시계(9개 DigitalClock, y=225/227/229)/오전오후(y=237)/초(y=227)를 전부 21-9 이전 원래 값으로 복귀: 시계 225→252/227→254/229→256, 오전오후 237→264, 초 227→254(초는 21-9에서 실제로 227이었던 걸 254로 복귀 — 지시서상 274 가정과는 다름, 원래 상태 그대로 되돌림)
- 날짜행(PartText)과 요일(PartText 7개) y=209→**227**로 18px 하향(x/width/height/seam x좌표 등 나머지 불변)
- 빌드/재설치(Favorite Id 29) 후 실측:
  - 메인시계 ink 상단 y=281(원위치로 정상 복귀 확인)
  - 날짜 ink y[242-266], 시계와 간격=**15px**(목표치와 정확히 일치)
  - 날씨아이콘 하단 y=217, 날짜 상단(242)과 간격=**25px**(날짜를 아래로 내린 덕에 오히려 여유 더 커짐, 겹침 없음 확인)
- 인터랙티브+AOD 스크린샷 재검증 완료

## 날짜/요일 폰트 과대 보정 (2026-08-28)
- 21-5단계에서 20%→3.7%로 축소 조정했던 size=28이 실제로는 데모 대비 약 12% 커진 것으로 재실측됨(사용자 확인)
- 날짜 PartText + 요일 PartText 7개(색상별 분기) 전부 size=28→**25**로 약 11% 축소(CLAUDE.md 5-10 "날짜/요일 폰트 크기 동일" 유지, 둘 다 동일하게 적용)
- 빌드/재설치(Favorite Id 30) 후 스크린샷으로 육안 확인 — 날짜 글자 크기가 주변 텍스트(요일·온도 등)와 비례가 자연스러워짐. 인터랙티브+AOD 재검증 완료

## 날씨아이콘 박스 height 90→105 재확대 (2026-08-28)
- 21-10에서 날짜행을 18px 내린 결과 아이콘-날짜 간격이 32px로 과도하게 벌어진 것 확인
- 고정 박스(x=0,y=121,width=438) height를 90→**105**로 복귀(=CLAUDE.md 5-2 원 스펙값과 동일), 32종 전부 동일 공식(height=105 고정, width=원본×105/원본높이, x=219-width/2 중앙정렬)으로 재계산
- 빌드/재설치(Favorite Id 31) 후 실측: 아이콘 하단 y=232, 날짜 상단 y=243 → 간격 **11px**(목표 9px 대비 2px 근접)
- 인터랙티브+AOD 재검증 완료, 아이콘 확대되고 날짜와 겹침 없음 확인

## 초(seconds) 크기 확대 + 하단정렬 보정 (2026-08-28)
- 1차 시도(size 50→65, 30% 단순비례)는 실측 잉크높이가 오히려 50px로 과대해지고 폭도 넓어져 원형 베젤에 "9"가 실제로 잘리는 문제 발생(줌 캡처로 육안 확인) → 이 폰트는 size 대비 ink 비율이 선형이 아님을 재확인, size 51로 재조정해 잉크높이 41px 확보(목표 39px에 근접, 베젤 클리핑 해소)
- 정렬: verticalAlign="BOTTOM"이 이미 적용돼 있었지만 박스 자체(y=254,height=70, 바닥=324)의 바닥이 메인시계 실제 잉크 하단(y=338)보다 위에 있어 "바닥정렬"이 무의미했던 것 확인 → 박스 y=254→**270**(height=70 유지, 바닥=340)로 내려서 재정렬
- 최종 실측(Favorite Id 34): 초 ink y[299-339] 높이41px, 메인시계 ink 하단 y=338 → 간격 -1px(사실상 동일선, 바닥정렬 완료), 가로 클리핑 없음(x324~372, 원형 베젤 안쪽)
- 인터랙티브+AOD 재검증 완료(AOD는 초 자체가 숨김 대상이라 메인시계만 확인, 영향 없음)

## 초(seconds) 박스 폭 클리핑 — 최초 판단 오류 정정 (2026-08-28)
- **1차 검증(오판)**: `adb root`+`date -s`로 00/08/09/44/48/58/11초 강제 재현 후 색상 임계값 기반 ink 폭 측정(46~50px)만으로 "박스(width49) 밖으로 넘쳐도 안 잘림"이라 결론 → **틀림**
- 사용자 지적으로 재검증: 고품질 보간(HighQualityBicubic) 확대 크롭으로 다시 보니 "09"/"00" 등에서 **0과 9의 오른쪽 닫는 획이 아예 없이 "C"자 모양으로 잘려있는 것**을 직접 확인. 1차 검증 때는 저해상도 nearest-neighbor 확대라 판독을 잘못함
- ASCII맵으로 정밀 확인한 결과 x372 부근에서 우측 스트로크가 완전히 사라짐(원형 베젤은 그 지점에서 한참 떨어져 있어 베젤 문제 아님) → **TimeText는 PartText/PartImage와 달리 선언된 width를 실제로 클리핑한다**는 새로운 사실 확인(기존 "WFF는 width로 안 잘라낸다"는 가정이 TimeText에는 적용 안 됨)
- 수정: 초 DigitalClock/TimeText width 49→**70**으로 확장(x=311 좌측 기준 유지, 메인시계와의 정렬 불변)
- 재검증: 00/09/44/48초 강제 재현 + 고품질 확대로 "01"/"10"/"45"/"49" 전부 닫힌 루프 포함 완전한 숫자 모양으로 정상 렌더링 확인
- 인터랙티브+AOD 재검증 완료(Favorite Id 35)

## 신규 요소: 오늘 최저/최고 기온 추가 (2026-08-28)
- 위치 x=0,y=28,438x17(가로/세로 중앙정렬), 민트 `#C9F7DC`+다크아웃라인, notosanskr_bold size15
- `[WEATHER.DAYS.0.TEMPERATURE_LOW]`/`[WEATHER.DAYS.0.TEMPERATURE_HIGH]`(공식 WFF 문법, 사용자 제시값 `WEATHER.DAY_TEMPERATURE_*`는 오표기였음 — 공식 문서로 `WEATHER.DAYS.<N>.*` 형식 확인 후 정정 적용) + `WEATHER.TEMPERATURE_UNIT` 화씨→섭씨 변환(기존 현재온도 블록과 동일 패턴), `[WEATHER.DAYS.0.IS_AVAILABLE]`로 로드 실패 시 "--" 표시
- **최초 시도 시 설치 자체 거부(FavoriteOperationException, NPE)** — CLAUDE.md 5-2 표를 함께 확인한 사용자가 "WFF v2도 예보 기능 지원한다"고 정정해줘서 버전 문제가 아님을 전제로 재조사
- **원인 규명 과정(최소단위 재현)**: ①IS_AVAILABLE 없이 DAYS.0 LOW/HIGH 단독 → 정상 ②화씨/섭씨 변환 Condition 추가 → 정상 ③IS_AVAILABLE을 `== true` 비교로 추가 → 정상 ④주석을 포함한 "최종본"으로 정리하며 재시도 → **다시 크래시** ⑤IS_AVAILABLE을 bare boolean으로 되돌려도(비교연산자 제거) 크래시 없이 정상
- **진짜 원인**: WFF 문법과 무관하게, XML 주석 안에 리터럴 `"--"`(큰따옴표로 감싼 대시 두 개)를 그대로 적어 넣은 것이 XML 스펙상 주석 내 금지 시퀀스라 시스템 파서가 깨짐 → 처음 크래시부터 마지막 재현까지 전부 이 주석 문제였고, `IS_AVAILABLE` bare boolean은 애초부터 `WEATHER.IS_DAY`처럼 정상 동작했음(비교연산자 불필요) — 중간에 "비교연산자 필요"로 잘못 결론 내렸던 것 정정
- 최종 코드: bare boolean 유지, 주석에서 리터럴 `--` 전부 제거
- 빌드/설치 반복(Favorite Id 37~42, 성공/실패 교차 확인) 끝에 최종본 정상 설치·활성화 확인, 인터랙티브("--" 표시, 에뮬레이터 데이터 없음)+AOD(숨김) 재검증 완료
- CLAUDE.md 5-2 좌표표에 "최저/최고 기온" 행 추가

## 최저/최고 기온 "--" 제거, 기존 온도 방식으로 통일 (2026-08-28)
- 사용자가 참조 스크린샷(wf_mintest.png, IS_AVAILABLE 로직 없던 최소 테스트본)을 지목 — 실제 기존 현재온도 블록도 "--"가 아니라 데이터 없을 때 그냥 "0°C"로 표시되는 게 기존 동작이라, 최저/최고도 동일하게 맞추는 게 맞다고 판단해 IS_AVAILABLE 기반 대시 오버라이드 제거
- 코드: 바깥쪽 IS_AVAILABLE Condition/Default 통째로 제거, 화씨/섭씨 변환 Condition만 남김(기존 WEATHER.TEMPERATURE 블록과 완전히 동일한 구조)
- **같은 실수 재발**: 주석에 리터럴 큰따옴표+대시 두 개를 다시 적어 넣어 또 FavoriteOperationException 크래시 발생 → 즉시 알아채고 주석에서 해당 표현만 제거해 재해결(빌드 실패 1회, Favorite Id 43에서 정상 설치 확인)
- 최종 확인: "최저 0° · 최고 0°" 참조 스크린샷과 동일하게 표시, AOD 숨김 정상

## 신규 요소 4종: 알림점 이동/알림개수/최고최저 확정/강수확률 (2026-08-28)
- 알림 점: x214,y8 → **x30,y214**(좌측 콤플리케이션 바깥, 원 세로중앙)로 이동
- 알림 개수 신규: x10,y226,50x14, ALIGN_CENTER, `[UNREAD_NOTIFICATION_COUNT]` 바인딩, 점과 완전히 동일한 Condition(has_unread) 안에 같이 넣어 표시/숨김 100% 동기화
- 최고/최저 기온: 기존 구현(x0,y28,438x17, `WEATHER.DAYS.0.TEMPERATURE_LOW/HIGH`) 유지 — **필드명 재확인**: 사용자가 `WEATHER.DAY_TEMPERATURE_LOW/HIGH`로 강행 요청했으나, 이미 실측 검증된 `WEATHER.DAYS.0.*`가 공식 문법이고 전자는 공식 문서 어디에도 없는 이름이라 사용자에게 확인 요청 → **DAYS.0.* 유지로 결정**(사용자 승인)
- 강수확률 신규: 라벨(x374,y194,60x14,"강수확률")+아이콘(x387,y198,34x31)+값(x374,y224,60x16,"{value}%") — 필드는 사용자 지정 `WEATHER.CHANCE_OF_PRECIPITATION`이 아니라 공식 문서 확인된 `WEATHER.DAYS.0.CHANCE_OF_PRECIPITATION`(DAYS 배열 하위)으로 정정 적용(온도와 동일한 배열 규칙)
- **우산 아이콘 리소스 처리**: 사용자가 채팅에 이미지를 붙여넣었으나 대화 첨부 이미지는 파일 바이트에 직접 접근 불가 확인 → 사용자가 `src/main/design/rain_umbrella_raw.png`(260x236, 투명배경)로 저장 후 처리. 로컬에 cwebp/ImageMagick/Python 전부 없어서 **Node.js(이미 설치돼있던 v24) + npm으로 scratchpad에 `sharp` 패키지 임시 설치**해 변환 파이프라인 구성: 투명 여백 트림(260x236→204x199 확인)→세로 62px 기준 리사이즈(64x62)→무손실 WebP 변환→`watchface/src/main/res/drawable/rain_umbrella.webp` 배치
- 빌드/설치(Favorite Id 44) 성공, 인터랙티브 스크린샷으로 강수확률(라벨+아이콘+"0%") 정상 렌더 확인, AOD 숨김 확인
- **알림점/개수 실측 미검증**: `adb shell cmd notification post`로 테스트 알림을 시도했으나 `UNREAD_NOTIFICATION_COUNT`가 반응하지 않음(에뮬레이터의 셸 알림이 워치 알림 카운트 소스에 실제로 집계되지 않는 것으로 추정) — 기존 알림점과 완전히 동일한 표현식/조건을 그대로 재사용한 코드라 로직 자체는 안전하다고 판단되지만, **실제 알림 표시 여부는 실기기에서 최종 확인 필요**(CLAUDE.md 6-6 GPS/날씨와 동일한 에뮬레이터 한계 유형)

## 강수확률 y좌표 겹침버그 수정 + 알림점/개수 강제표시 검증 (2026-08-28)
- 강수확률 라벨/아이콘/값 y좌표만 수정(x/width/height/내용/색상 불변): 라벨 194→186, 아이콘 198→203, 값 224→237
- 알림점(x30,y214,10x10)/알림개수(x10,y226,50x14) 실제 겹침 여부 검증: `UNREAD_NOTIFICATION_COUNT` 조건을 임시로 `1>0`(항상 참)으로, 값 파라미터를 임시로 `3`으로 강제 설정해 빌드·설치 후 확대 캡처로 확인 → 점과 숫자가 겹치지 않고 위아래로 명확히 분리되어 표시됨 확인, 검증 후 원래 바인딩(`[UNREAD_NOTIFICATION_COUNT]`)으로 즉시 원복
- 빌드/재설치(Favorite Id 45=강제표시 테스트, 46=최종 원복본) 완료, 인터랙티브+AOD 재검증 완료(강수확률 3요소 간격 정상, 알림점/개수 정상 동작 확인, 그 외 요소 전부 불변)

## 이슈/보류 메모
- 스트레스 지수: 구현 시도 결과 → (성공/실패 기록)
- 폰배터리 슬롯(SLOT_MID_LEFT): 사용자 앱 지정 테스트 결과 →
- 온도 섭씨 강제: 구현 가능 여부 →
- 예비 이미지 자원(03/04/12/14/15) 활용 여부 →
